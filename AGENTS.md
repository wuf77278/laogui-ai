# 设计师网站项目底层规则

以下规则适用于本项目中的所有开发、修改、测试和启动操作，任何 Agent 都必须遵守。

## 一、最新版本的唯一判断标准

1. 本项目当前文件夹 `/Users/Apple_501/Desktop/设计师网站` 是唯一源码来源。
2. “最新版本”是这个文件夹中的完整当前状态，包括：已经提交的改动、尚未提交的改动和新增文件。不能只按最近一次 Git 提交判断。
3. 不得覆盖、丢弃、回退或忽略用户已有改动。发现未提交内容时，必须保留并在其基础上继续工作。
4. 启动前必须检查 `git status --short`、最近的提交记录和可用的暂存备份。如果备份中可能有更新功能，先比较并按功能合并，不能直接用旧文件覆盖新文件。

## 二、功能同步规则

1. 用户新增或修改的功能必须及时合并到当前源码，不能留在旧安装版、临时副本、其他分支或暂存备份中。
2. 每次改动功能后，都要检查与它相关的页面、服务端、说明文档和测试，确保需要联动的部分已经同步。
3. 已经存在的功能不能因合并新功能而消失。完成修改前要检查主要功能入口仍然存在。

## 三、启动项目规则

1. 当用户要求“启动项目”时，默认启动本文件夹中的最新源码。
2. 默认启动命令为：

   ```bash
   cd /Users/Apple_501/Desktop/设计师网站
   npm start
   ```

3. 除非用户明确要求，否则禁止启动 `/Applications/老鬼AI.app` 或其他旧安装版、旧副本。
4. 如果 `4177` 端口已被占用，必须先查清占用进程。只关闭确认属于本项目旧服务或旧安装版的进程，不能误关其他程序，然后重新启动当前源码。
5. 启动后必须实际确认：
   - `http://127.0.0.1:4177` 可以访问；
   - 当前进程来自本项目目录；
   - 本轮新增或修改的主要功能已经由正在运行的页面加载；
   - 项目的服务状态正常。
6. 未完成以上检查时，不能向用户说“最新版本已经启动”。

## 四、完成前检查

1. 根据改动范围运行语法检查、项目自检和自动测试。
2. 明确区分“代码已经写入”和“正在运行的服务已经加载”，修改服务端文件后必须重启服务。
3. 最终用简单中文告诉用户：启动地址、已确认的主要功能、测试结果和无法完成的事项。
