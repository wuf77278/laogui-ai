# 实施计划：设计行迹第一版

1. 独立建立 React、TypeScript、Vite、Cesium 工程，保存旧原型。
2. 建立项目类型、筛选逻辑、本地 JSON 仓库和自动测试。
3. 完成只显示中国的 Cesium 地图、点位、详情和时间轴。
4. 完成中文后台、上传、备份和恢复。
5. 完成电脑、手机、构建和浏览器验收。

每个阶段都必须通过类型检查、测试和运行验证，不修改“老鬼AI”主程序。
