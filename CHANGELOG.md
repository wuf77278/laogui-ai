# 更新记录

## 2.3.16 - 2026-08-15

- AI 编辑继续固定使用老鬼AI内置图片内核，Windows 安装验收新增一次真实蒙版编辑，不再只检查文件存在。
- AI 编辑和深度编辑的大图结果改为按内容去重落盘，避免同一张图在画布状态里重复保存多份。
- 七天日志清理覆盖内置图片内核的请求目录，服务启动后会定时清理过期日志。
- Mac、Windows 安装包只保留当前平台内核，删除错误架构的旧兼容文件。
- 首页循环视频由 4K 60 帧压缩为 1080p 30 帧，三个视频总计由约 102MB 降至约 11MB。
- 修复正式依赖中的 `js-yaml` 高风险漏洞，更新检查提示统一为 Gitee。
- 发布流程要求 Windows 正式签名证书及 Mac Developer ID、公证凭据；缺失时停止发布，避免把未签名包发给朋友。

## 2.3.15 - 2026-08-15

### Mac 与 Windows 生图内核统一

- Mac 和 Windows 的普通生图、参考图生图与 AI 蒙版编辑统一使用软件内置图片引擎。
- 老鬼 AI 不再调用个人目录中的聚合生图脚本，也不再依赖系统 Python 版本。
- 生图接口地址、Key、模型和兼容参数只读取老鬼 AI 自己的设置，避免外部软件配置串入。
- 增加跨平台回归检查，防止后续版本重新引入外部 Python 或聚合生图依赖。

## 2.3.14 - 2026-08-13

### Windows AI 编辑修复

- 修复空的聚合生图脚本路径被误判为程序目录的问题。
- Windows 缺少 Python 或外部聚合生图脚本时，自动使用软件内置图片引擎，不再误报 502。
- 增加 Windows 回归检查，避免后续版本再次错误调用 `python3`。

## 2.3.13 - 2026-08-13

### 电脑版兼容性修复

- 修复 Windows 版 AI 编辑错误寻找 Mac 专用聚合生图脚本的问题。
- Windows 和没有外部聚合生图脚本的安装环境，自动使用随软件打包的跨平台图片引擎。
- AI 编辑的原图、参考图和 PNG 蒙版统一交给内置引擎处理，Mac 仍保留原有聚合生图脚本调用。
- 补充内置引擎不可用时的明确错误信息，避免把本地配置缺失误显示成网络 502。

## 2.3.7 - 2026-08-07

### 电脑版

- 新增批量生图任务的并行处理、进度汇总和“停止本轮生成”操作，已完成的结果会保留。
- 支持取消后台任务，并在任务日志中区分成功、失败和已取消。
- 增加任务日志筛选和失败诊断导出，导出内容会隐藏接口密钥。
- 优化任务断线后的后台结果恢复、任务状态更新和错误提示。

### 手机端

- 同步最新手机端画布、AI 编辑和接口配置源码。
- 本轮只同步源码，不发布手机端安装包或应用商店版本。

## 2.3.6 - 2026-08-03

### 画笔优化

- AI 编辑的画笔补选和画笔减选支持拖动时实时显示轨迹。
- 深度编辑的修边画笔、绘画和擦除支持拖动时实时显示轨迹。
- 画笔和橡皮擦显示与实际涂抹范围一致的圆形光标。
- 新增细、中、粗、特粗四档快捷粗细，并支持滑杆精细调整。
- 粗细面板只在使用相关工具时展开，切换其他工具后自动收起。
- 统一画笔大小为实际直径，解决相同数值下显示范围与实际范围不一致的问题。

### 项目同步

- 同步手机版基础工程和聚合生图接口配置生成脚本。
- 优化生图内核相关提示文字，方便朋友电脑理解和配置。

## 2.3.5 - 2026-07-24

### 修复

- 上传单张素材参考图后，不再强制进入 AI 编辑页面。
- 暗夜模式下的参考图选项和空画布不再出现白框。

### 优化

- 参考图卡片升级为磨砂玻璃样式，重新整理参考强度和重点参考内容。
- 增加明确的“局部编辑”按钮，需要时再手动进入编辑页面。
- 优化白天、暗夜和窄屏下的参考图卡片布局。

## 2.3.4 - 2026-07-23

- 新增多套生图 API 调用优先级设置。
- 首选 API 失败后，自动依次尝试后续 API。
- 支持从 Markdown、文本和 JSON 配置中读取 API 优先级。
- API 设置页支持编辑顺序，并显示当前调用优先级。

## 2.2.1 - 2026-07-17

### 修复

- 软件启动时不再自动检查或下载更新。
- 用户手动检查到新版本后，需要点击“下载并更新”并确认才会继续。
- Windows 和 Mac 改为顺序发布，避免 GitHub 生成重复版本页。

## 2.2.0 - 2026-07-14

### 新增

- 可保存、切换和管理多套生图 API 配置。
- 每套 API 独立显示连接状态、检测时间和延迟。
- 支持从粘贴文本、JSON、`.env`、文件或文件夹导入 API 配置。
- 支持导出分享文件或复制分享文本，方便同事直接导入。

### 优化

- FHL 接口默认优先使用流式返回，降低长时间等待。
- 失败时不再盲目重复请求，减少意外重复扣费和超长任务。
- 更新 Electron 和打包工具，修复已知安全问题。

### 安全提示

- 导出的 API 分享文件包含完整 API Key，只能发给可信任的同事。
